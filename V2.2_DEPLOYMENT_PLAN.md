# 🐱 小肥猫学习 v2.2 部署验证方案

> 部署日期: 2026-05-18 | 目标: JumpServer云服务器

---

## 一、版本变更摘要 (v2.1 → v2.2)

### 核心新增功能

| 序号 | 功能 | 涉及文件 | 说明 |
|------|------|---------|------|
| A | 图片格式自动检测 | `feishu_api.py` | 修复硬编码 `image/jpeg` 导致的400错误 |
| B | Token缓存复用 | `main.py`, `feishu_api.py` | `_download_image` 复用 `_get_tenant_token()` 缓存 |
| C | 多图片批次收集 | `main.py` | 60秒窗口自动聚合图片批次 |
| D | 图片区段匹配 | `grading.py` | AI识别每张图覆盖的题目区域 |
| E | 部分批改支持 | `grading.py` | 支持分批提交，显示进度条 |
| F | 逐日完成追踪 | `grading.py` | 前一日未完成则阻止今日批改 |
| G | 2026新版数学教材 | `question_generator.py` | MATH_TOPICS完全重写 |

### 2026教材重大变化

```
删除: 面积(第四单元)、认识分数(第六单元)、时间·年月日(第三单元)
新增: 动手做、关系与规律、动物体重说明书、图书排序、家庭旅行计划
变化: 乘法→整数乘法(一)、除法→整数除法(一)、图形运动→正式第二单元
```

---

## 二、部署前检查清单

### 2.1 代码变更确认

| 检查项 | 命令 | 预期结果 |
|--------|------|---------|
| 代码无语法错误 | `python3 -m py_compile cloud_function/ws-server/feishu_api.py` | OK |
| 代码无语法错误 | `python3 -m py_compile cloud_function/ws-server/main.py` | OK |
| 代码无语法错误 | `python3 -m py_compile cloud_function/ws-server/grading.py` | OK |
| 代码无语法错误 | `python3 -m py_compile question_generator.py` | OK |
| 导入无循环依赖 | `python3 -c "from cloud_function.ws-server.grading import *"` | OK |

### 2.2 环境变量确认

```bash
# JumpServer服务器上检查
echo $FEISHU_APP_ID          # 应有值
echo $DEEPSEEK_API_KEY       # 应有值
echo $BITABLE_APP_TOKEN      # 应有值
echo $BITABLE_DAILY_TABLE_ID # 应有值
echo $BITABLE_MISTAKE_TABLE_ID # 应有值
echo $USER_OPEN_ID           # ou_8bf3770ed43ce0f273c7a34f1597cfe9
```

### 2.3 依赖确认

```bash
pip list | grep -E "lark-oapi|openai|requests"
# 预期: lark-oapi >= 1.x, openai >= 1.x, requests >= 2.x
```

---

## 三、部署步骤

### Step 1: 停止旧服务

```bash
# JumpServer服务器
sudo systemctl stop xiaofeimao-ws
sudo systemctl status xiaofeimao-ws  # 确认已停止
```

### Step 2: 备份旧版本

```bash
cd /opt/xiaofeimao-cloud
cp -r cloud_function/ws-server cloud_function/ws-server.bak_$(date +%Y%m%d_%H%M)
cp question_generator.py question_generator.py.bak_$(date +%Y%m%d_%H%M)
cp grading_rules.json grading_rules.json.bak_$(date +%Y%m%d_%H%M)
```

### Step 3: 部署新代码

```bash
# 从Mac上传代码到JumpServer (通过scp或JumpServer文件管理)
scp -r cloud_function/ws-server/feishu_api.py user@jumpserver:/opt/xiaofeimao-cloud/cloud_function/ws-server/
scp -r cloud_function/ws-server/main.py user@jumpserver:/opt/xiaofeimao-cloud/cloud_function/ws-server/
scp -r cloud_function/ws-server/grading.py user@jumpserver:/opt/xiaofeimao-cloud/cloud_function/ws-server/
scp question_generator.py user@jumpserver:/opt/xiaofeimao-cloud/

# 注意: grading_rules.json 和 daily_progress.json 无需覆盖(是运行时数据)
```

### Step 4: 启动服务

```bash
sudo systemctl start xiaofeimao-ws
sudo systemctl status xiaofeimao-ws  # 确认 Active: active (running)

# 查看启动日志
sudo journalctl -u xiaofeimao-ws -f -n 50
# 预期看到: "🔗 正在建立 WebSocket 长连接到飞书..."
#           "🐱 小肥猫学习 - WebSocket 长连接批改服务"
```

### Step 5: 健康检查

```bash
# 确认进程存在
ps aux | grep "python.*main.py"

# 确认WebSocket连接成功
sudo journalctl -u xiaofeimao-ws | grep -i "websocket\|连接"

# 确认无启动错误
sudo journalctl -u xiaofeimao-ws | grep -i "error\|traceback\|exception" | tail -5
# 预期: 无错误输出
```

---

## 四、验证测试用例

### 测试A: 图片上传格式修复

**测试目的**: 验证PNG/JPEG等不同格式图片上传不再400报错

```
前置条件: 飞书机器人已启动
测试步骤:
  1. 在飞书中向小肥猫发送一张PNG格式的作业图片
  2. 观察日志: sudo journalctl -u xiaofeimao-ws -f

预期结果:
  ✅ 日志显示 "图片格式检测: image/png (文件名: image.png)"
  ✅ 日志显示 "图片上传成功: image_key=..."
  ✅ 日志显示 "图片消息已发送: ..."
  ❌ 不应出现 "上传图片失败: code=..." 或 HTTP 400 错误
```

### 测试B: 多图片批次收集

**测试目的**: 验证60秒内连续发送的图片被归入同一批次

```
前置条件: Bitable中有当日题目记录
测试步骤:
  1. 在飞书中连续发送3张作业图片(间隔<60秒)
  2. 观察日志

预期结果:
  ✅ 第1张: "📸 收到图片，正在加入批次...（60秒内可继续发图）"
  ✅ 第2张: "📸 第2张图片已加入批次（共2张）"
  ✅ 第3张: "📸 第3张图片已加入批次（共3张）"
  ✅ 60秒后: "[批次] 定时器触发，开始处理 3 张图片"
  ✅ 收到批改结果卡片
```

### 测试C: 图片区段匹配与部分批改

**测试目的**: 验证多张不同区域的图片能正确匹配到对应题目

```
前置条件: 当日有数学+英语混合题目(如M1-M4, E1-E4)
测试步骤:
  1. 图1: 拍数学题M1-M4区域
  2. 图2: 拍英语题E1-E4区域
  3. 在60秒内连续发送两张图

预期结果:
  ✅ 日志显示 "[区段匹配] 共2张图，匹配结果: 图1覆盖数学前4题,图2覆盖英语全部4题"
  ✅ 批改结果卡片包含所有8道题的批改
  ✅ 进度条显示 "██████████ 100%（8/8）"
```

### 测试D: 逐日前置检查

**测试目的**: 验证前一天未完成时阻止今日批改

```
前置条件: 前一天(假设5月17日)有题目但未完成批改
测试步骤:
  1. 在飞书中发送今日(5月18日)的答案文本

预期结果:
  ✅ 收到阻止消息: "⛔ 每日练习规则"
  ✅ 消息包含 "📅 你必须先完成 2026-05-17 的练习"
  ✅ 列出前一天剩余题目
  ✅ 本次批改未执行
```

### 测试E: 进度查询

**测试目的**: 验证"查看进度"指令

```
测试步骤:
  1. 发送文本 "查看进度"

预期结果:
  ✅ 收到进度消息: "📊 2026-05-18 练习进度"
  ✅ 显示总题数、已完成、待完成数量
  ✅ 如全部完成，显示 "🎉 今日练习已全部完成！"
```

### 测试F: 数学出题准确性(2026新版)

**测试目的**: 验证新生成的题目使用2026新版教材知识点

```
测试步骤:
  1. 执行: python3 question_generator.py --preview
  2. 检查输出JSON中的 math.topic 字段

预期结果:
  ✅ topic 值为2026新版知识点(如"两位数×两位数竖式计算")
  ✅ 不含旧教材内容(面积、分数、年月日)
  ✅ 每日循环正确(15天一轮)
  ❌ 不应出现 "面积单位"、"分数"、"24时计时法" 等旧话题
```

### 测试G: 端到端完整流程

**测试目的**: 全链路验证

```
测试步骤:
  1. 执行每日出题(或使用已有今日题目)
  2. 发送答案文本(如 "83 44 63,22 forget arrive")
  3. 拍照发送图片
  4. 发送"查看进度"
  5. 发送"查看规则"
  6. 发送"调整：翻译题意思对即可"

预期结果:
  ✅ 文本批改正常返回结果卡片
  ✅ 图片批改正常返回结果(含OCR识别+匹配)
  ✅ 进度显示正确
  ✅ 规则修改生效
```

---

## 五、监控指标

### 5.1 关键日志关键词

| 日志模式 | 含义 | 告警级别 |
|---------|------|---------|
| `[ERROR]` | 任何错误 | P1 |
| `上传图片失败` | 图片上传异常 | P1 |
| `[批次失败]` | 批次处理失败 | P2 |
| `[异常] 事件处理失败` | 消息处理崩溃 | P0 |
| `DeepSeek API 调用失败` | AI服务不可用 | P0 |
| `token 已刷新` | Token正常轮转 | Info |
| `[批次完成]` | 批次处理成功 | Info |

### 5.2 性能指标

- WebSocket连接: 应保持稳定，无频繁断连
- OCR识别延迟: 单张图片 < 5秒
- 批改响应时间: 文本 < 10秒，图片批次 < 30秒
- Token刷新频率: 每~2小时一次

---

## 六、回滚方案

### 快速回滚(5分钟内)

```bash
# Step 1: 停止新版本
sudo systemctl stop xiaofeimao-ws

# Step 2: 恢复旧代码
cd /opt/xiaofeimao-cloud
cp cloud_function/ws-server.bak_YYYYMMDD_HHMM/feishu_api.py cloud_function/ws-server/
cp cloud_function/ws-server.bak_YYYYMMDD_HHMM/main.py cloud_function/ws-server/
cp cloud_function/ws-server.bak_YYYYMMDD_HHMM/grading.py cloud_function/ws-server/
cp question_generator.py.bak_YYYYMMDD_HHMM question_generator.py

# Step 3: 启动旧版本
sudo systemctl start xiaofeimao-ws

# Step 4: 验证
sudo systemctl status xiaofeimao-ws
sudo journalctl -u xiaofeimao-ws -n 20
```

### 回滚触发条件

- 连续3次以上图片上传失败(400/500)
- WebSocket连接反复断开
- 批改结果严重错误(连续3次以上)
- DeepSeek API持续超时

---

## 七、部署后观察期

| 时间 | 检查项 |
|------|--------|
| 部署后5分钟 | 检查日志无ERROR，WebSocket连接正常 |
| 部署后30分钟 | 发送测试消息验证文本批改正常 |
| 部署后1小时 | 发送图片验证图片批改正常 |
| 部署后24小时 | 检查当日所有批次处理成功率 |
| 部署后48小时 | 验证逐日追踪规则正确执行 |

---

## 八、已知限制与TODO

| 项目 | 状态 | 说明 |
|------|------|------|
| 题目内容按日期混用 | ⚠️ 注意 | 出题使用新教材循环，但Bitable中旧日期的题目仍是旧教材内容 |
| `daily_progress.json` 兼容性 | ⚠️ 注意 | 新版本进度文件格式兼容v2.1，但旧进度数据需要手动清理 |
| 图片下载token | ✅ 已修复 | v2.2已复用token缓存 |
| 图片MIME类型 | ✅ 已修复 | v2.2已自动检测5种格式 |
| 错题本旧数据 | ⚠️ 注意 | 旧教材的错题仍会出现在错题复习中，建议按日期筛选 |

---

> 📋 方案版本: v1.0 | 编写日期: 2026-05-18 | 适用版本: v2.2
